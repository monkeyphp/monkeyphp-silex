{# src: resources/templates/about/index.twig #}
{% extends "layout.twig" %}

{% block content %}

<section>
    
    <h2>About</h2>
    
    {% if about is defined %}
        {% if about.body is defined %} 
        {% autoescape false %}
        {{ about.body|markdown }}
        {% endautoescape %}
        {% endif %}
    {% endif %}
    
</section>

{% endblock %}
    