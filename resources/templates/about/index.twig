{# src: resources/templates/about/index.twig #}
{% extends "layout.twig" %}

{% block content %}

    {% if about is defined %}
        {% if about.body is defined %} 
        {% autoescape false %}
        {{ about.body|markdown }}
        {% endautoescape %}
        {% endif %}
    {% else %}
        
    {% endif %}

{% endblock %}