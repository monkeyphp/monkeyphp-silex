{# src: resources/templates/admin/message/delete.twig #}

{% extends "layout.twig" %}

{% block content %}

<section>
    
    {{ form_start(form) }}
    
    {{ form_widget(form.id) }}
    
    <div>
        {{ form_widget(form.delete) }}
    </div>
    
    <div>
        {{ form_widget(form.cancel) }}
    </div>
    
    {{ form_end(form) }}
    
</section>

{% endblock %}
